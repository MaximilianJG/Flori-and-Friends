<div class="content">
  <h1 class="pt-3">Das Team</h1>

  <p>Sie haben selbst nichts, aber geben alles für das Wohl  der Tiere. In einem Land, wo die Not unserer Mitgeschöpfe groß ist,
  sind sie seit Jahren auf der Seite dieser leidgeplagten Wesen und schenken ihnen Zuflucht, Fürsorge und Liebe – das
  Flori & Friends Team vor Ort: Flori, Andreas, Marius, Mihaela und Viorel </p>

  <div class="row">
    <div class="col-3 mt-3">
      <% @user = User.where(position: "Präsidentin - Verein Rumänien / Sheltermanagerin") %>
      <% unless @user[0].nil? %>
        <% if @user[0].photo.attached? %>
          <%= cl_image_tag @user[0].photo.key, width: "260px", height: "300px", class: "feature-image" %>
        <% else %>
            <%= cl_image_tag "Portrait_Placeholder_ry8542", width: "260px", height: "300px", class: "feature-image" %>
        <% end %><br>
        <p class="mt-2 text"> <strong><%= @user[0].position %>:</strong> <%= @user[0].first_name %> <%= @user[0].last_name %> </p>
      <% end %>
    </div>

    <div class="col-3 mt-3">
      <% @user = User.where(position: "Tierpfleger") %>
      <% unless @user[0].nil? %>
        <% if @user[0].photo.attached? %>
          <%= cl_image_tag @user[0].photo.key, width: "260px", height: "300px", class: "feature-image" %>
        <% else %>
            <%= cl_image_tag "Portrait_Placeholder_ry8542", width: "260px", height: "300px", class: "feature-image" %>
        <% end %><br>
        <p class="mt-2 text"> <strong><%= @user[0].position %>:</strong> <%= @user[0].first_name %> <%= @user[0].last_name %> </p>
      <% end %>
    </div>

    <div class="col-3 mt-3">
      <% @user = User.where(position: "Tierpfleger") %>
      <% unless @user[1].nil? %>
        <% if @user[1].photo.attached? %>
          <%= cl_image_tag @user[1].photo.key, width: "260px", height: "300px", class: "feature-image" %>
        <% else %>
            <%= cl_image_tag "Portrait_Placeholder_ry8542", width: "260px", height: "300px", class: "feature-image" %>
        <% end %><br>
        <p class="mt-2 text"> <strong><%= @user[1].position %>:</strong> <%= @user[1].first_name %> <%= @user[1].last_name %> </p>
      <% end %>
    </div>

    <div class="col-3 mt-3">
      <% @user = User.where(position: "Tierpfleger") %>
      <% unless @user[2].nil? %>
        <% if @user[2].photo.attached? %>
          <%= cl_image_tag @user[2].photo.key, width: "260px", height: "300px", class: "feature-image" %>
        <% else %>
            <%= cl_image_tag "Portrait_Placeholder_ry8542", width: "260px", height: "300px", class: "feature-image" %>
        <% end %><br>
        <p class="mt-2 text"> <strong><%= @user[2].position %>:</strong> <%= @user[2].first_name %> <%= @user[2].last_name %> </p>
      <% end %>
    </div>

    <div class="col-3 mt-3">
      <% @user = User.where(position: "Tierpfleger") %>
      <% unless @user[3].nil? %>
        <% if @user[3].photo.attached? %>
          <%= cl_image_tag @user[3].photo.key, width: "260px", height: "300px", class: "feature-image" %>
        <% else %>
            <%= cl_image_tag "Portrait_Placeholder_ry8542", width: "260px", height: "300px", class: "feature-image" %>
        <% end %><br>
        <p class="mt-2 text"> <strong><%= @user[3].position %>:</strong> <%= @user[3].first_name %> <%= @user[3].last_name %> </p>
      <% end %>
    </div>
  </div>
</div>
